% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_data.R
\name{sim_data}
\alias{sim_data}
\title{Simulate interval data for k groups using a Gaussian copula}
\usage{
sim_data(
  group_sizes,
  dist = c("gamma", "poisson"),
  center_par = list(mean = 5, scale = 2),
  radius_par = list(mean = 10, scale = 1),
  rho = 0.5
)
}
\arguments{
\item{group_sizes}{Integer vector. Number of observations per group.}

\item{dist}{Character string. Margin family: "gamma" (default) or "poisson".}

\item{center_par}{List of parameters for the center margin.
\itemize{
\item If \code{dist="gamma"}: \code{list(mean, scale)}. (Legacy: \code{disp} as alias of \code{scale}.)
\item If \code{dist="poisson"}: \code{list(mean)} used as \code{lambda}.
}}

\item{radius_par}{Same structure as \code{center_par} for the radius margin.}

\item{rho}{Numeric in (-1,1). Latent Gaussian correlation between center and radius.}
}
\value{
A list of data frames (one per group) with columns: \code{center}, \code{radius}, \code{group}.
}
\description{
Generates interval-valued data where each observation is defined by a center
and a radius. Dependence between center and radius is induced via a Gaussian
copula; margins can be Gamma or Poisson. For Poisson radii, zeros are bumped
to a tiny positive value to keep radii strictly > 0.
}
\examples{
## Example 1: Gamma margins with positive dependence
set.seed(2)
sim <- sim_data(
  group_sizes = c(10, 11, 9),
  dist        = "gamma",
  center_par  = list(mean = 5,  scale = 2),
  radius_par  = list(mean = 10, scale = 1),
  rho         = 0.5
)
head(do.call(rbind, sim))

## Example 2: Poisson margins with mild negative dependence
set.seed(123)
simP <- sim_data(
  group_sizes = c(6, 5, 7),
  dist        = "poisson",
  center_par  = list(mean = 5),   # lambda for centers
  radius_par  = list(mean = 8),   # lambda for radii
  rho         = -0.3
)
head(do.call(rbind, simP))
}
