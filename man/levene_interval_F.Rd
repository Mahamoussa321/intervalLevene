% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/levene_interval_F.R, R/stat_interval_levene.R
\name{levene_interval_F}
\alias{levene_interval_F}
\title{Interval-Levene F statistic}
\usage{
levene_interval_F(df, group = NULL, omega = 1)

levene_interval_F(df, group = NULL, omega = 1)
}
\arguments{
\item{df}{data.frame with columns \code{center}, \code{radius}, and (optionally) \code{group}.}

\item{group}{Factor (or coercible) of length \code{nrow(df)} giving group memberships.
If \code{NULL} and \code{df$group} exists, it will be used.}

\item{omega}{Nonnegative numeric weight on the radius component (default 1).}
}
\value{
Numeric F statistic (MSB/MSE). Returns \code{NA_real_} with a warning if SSE=0.

Numeric F statistic (MSB/MSE). Returns \code{NA_real_} with a warning if SSE=0.
}
\description{
Computes an ANOVA-style Levene statistic on interval-valued data by forming
\eqn{Y_{ij} = (C_{ij}-\bar C_i)^2 + \omega (R_{ij}-\bar R_i)^2} and returning
\eqn{F = \{SSB/(k-1)\}/\{SSE/(N-k)\}}.

Computes an ANOVA-style Levene statistic on interval-valued data by forming
\eqn{Y_{ij} = (C_{ij}-\bar C_i)^2 + \omega (R_{ij}-\bar R_i)^2} and returning
\eqn{F = \{SSB/(k-1)\}/\{SSE/(N-k)\}}.
}
\examples{
set.seed(1)
df <- data.frame(
  center = rnorm(60),
  radius = rexp(60),
  group  = factor(rep(1:3, each = 20))
)
levene_interval_F(df, df$group)
set.seed(1)
df <- data.frame(
  center = rnorm(60),
  radius = rexp(60),
  group  = factor(rep(1:3, each = 20))
)
levene_interval_F(df, df$group)
}
