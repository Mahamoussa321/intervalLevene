% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot_np.R
\name{bootstrap_interval_levene_np}
\alias{bootstrap_interval_levene_np}
\title{Nonparametric (case-resampling) bootstrap for the interval-Levene F}
\usage{
bootstrap_interval_levene_np(
  df,
  omega = 1,
  R = 2000,
  seed = NULL,
  alpha = 0.05,
  return_boot = FALSE
)
}
\arguments{
\item{df}{List of per-group data.frames (first two cols center/radius),
or one data.frame with \code{center}, \code{radius}, \code{group}.}

\item{omega}{Nonnegative weight on the radius component in Y. Default 1.}

\item{R}{Number of bootstrap replicates. Default 2000.}

\item{seed}{Optional integer seed.}

\item{alpha}{Significance level for the critical value (default 0.05).}

\item{return_boot}{If TRUE, return the full \code{boot::boot} object.}
}
\value{
list(F_obs, crit, p_value, R, boot?)
}
\description{
Uses a \strong{within-group nonparametric bootstrap}: resample rows \emph{within each
group} (preserving group sizes) and recompute the interval-Levene F to get
an empirical reference for critical values and p-values.
}
